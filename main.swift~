func insertionSort(_ array: [String]) {
    var arr = array
    for x in 1..<arr.count {
        var y = x
        while y > 0 && arr[y] < arr[y - 1] { //y > 0 for the first number so the index doesn't become negative and go out of range (nothing behind first index)
            arr.swapAt(y - 1, y)
            array2.swapAt(y - 1, y)
            y -= 1
   
            
        }
       
        

    }
   
    }



var array = [String]()
var array2 = [String]()
while let input = readLine() {
    
    array.append(input.lowercased())
    array2.append(input)

}
insertionSort(array2)
for x in array2 {
    print(x)
   
}
